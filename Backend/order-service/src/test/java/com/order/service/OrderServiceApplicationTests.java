package com.order.service;

import static org.junit.jupiter.api.Assertions.assertEquals;
import static org.mockito.Mockito.when;

import java.util.ArrayList;
import java.util.List;

import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.core.annotation.Order;


@SpringBootTest
class OrderServiceApplicationTests 
{
	/*@Mock
	private OrderRepository orderrepository;

	@InjectMocks
	private OrderResource orderresource;
	
	public List<Order> order;
	
	
	@Test 
	@Product(1)
	public void TestFindById(){ 
		List<com.order.service.Order> order = new ArrayList<Order>();
		order.add(new Order (2,"2022-04-02","Podili,prakasm dist,line 1","battulamanisha@gmail.com","986689897"));
		when(orderrepository.findById(2)).thenReturn(order);
		assertEquals(1, orderrepository.findById(2).size());
		

		
	} */
}



